# 프로젝트 규칙

## 범위

- 이 프로젝트는 React + TypeScript + Vite 기반의 Todo 학습 프로젝트입니다.
- 작업에서 명시적으로 요구하지 않는 한 기존 애플리케이션의 동작과 구조를 유지합니다.
- 기능 범위는 요청된 Todo 동작에 집중하며, 추측성 추상화와 의존성 추가를 피합니다.

## 프로젝트 구조

- `src/App.tsx`: 애플리케이션 구성과 상태 연결
- `src/components/`: UI 컴포넌트
- `src/todoState.ts`: 순수 Todo 상태 처리 함수
- `src/types/`: 공용 TypeScript 타입
- `src/App.test.tsx`: 기존 단위 테스트
- `e2e/`: Playwright E2E 테스트가 있을 경우 사용하는 디렉터리

## 개발 규칙

- 새 코드를 추가하기 전에 기존 컴포넌트, 상태 함수, 셀렉터를 우선 재사용합니다.
- 표준 라이브러리, 브라우저 기본 기능, 이미 설치된 의존성을 우선 사용합니다.
- 동작이 잘못되었거나 작업에서 명시적으로 요청한 경우가 아니면 테스트를 위해 운영 코드를 변경하지 않습니다.
- 테스트는 사용자의 관점에서 작성하며, 접근 가능한 라벨·역할·화면에 표시되는 텍스트를 통해 상호작용합니다.
- 별도 요청이 없는 한 `dist/`, `.test-dist/`, Playwright 리포트, 스크린샷 같은 생성 결과물은 커밋하지 않습니다.

## 검증

변경 후 전체 검증은 다음 명령 하나로 실행합니다.

```text
npm run verify
```

`verify`는 다음 검증을 순서대로 실행합니다.

```text
npm test
npm run lint
npm run build
npm run test:e2e
```

검증이 실패하면 근본 원인을 파악하고 최소한으로 수정한 뒤, 실패한 검증과 관련된 기본 검증을 다시 실행합니다.
